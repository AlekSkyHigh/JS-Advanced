// Write a JS function that takes one string parameter 
// and prints on two lines the length of the parameter and then the unchanged parameter itself.

function echoFunction(inputString) {

    console.log(inputString.length);
    console.log(inputString);
    
}

echoFunction('Hello, JavaScript!')

echoFunction('strings are easy')
